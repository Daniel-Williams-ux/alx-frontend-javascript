//only make promise
const getResponseFromAPI = new Promise((resolve, reject) => {
 
  if (true) {
    resolve();
  } else {
    reject();
  }
  /* eslint-enable */
})
export default getResponseFromAPI;
